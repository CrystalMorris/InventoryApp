<h3>{{thisItem.name}}</h3>

<img class="item-img" src="{{thisItem.image}}" />

<div class="row">
	<h5>{{thisItem.description}}</h5>
	<h5>Price: {{thisItem.price}}</h5>
	{{!-- <h5>Quantity: {{thisItem.quantity}}</h5> --}}
	<h5>Category: {{thisItem.Category.name}}</h5>
</div>
	<a href="/item/{{thisItem.id}}/delete">delete this item</a>
<br><br>
<div>
    <button type="button" class="button" onclick="goPrev()">Back to Inventory</button>
	<script>
		function goPrev()
		{
			window.history.back();
		}
	</script>
</div> 